# проблема
1. Запустили VAULT
2. занесли тула много секретов
3. Контейнер - перезапустился (pod-restart, node-restart и так далее)
4. VAULT поднялся
5. но он в состоянии: SEAL (запечатан)
6. Пока-что не сделать UNSEAL - ничего прочитать из него нельзя

# Решение
1. После инициализации VAULT - ключи + root_token сохранить как JSON файл на всех серверах kubernetes (control-plane). На уровне /.../kube-admin.conf
   1. по уровню критичности, эти два файла примерно равны
2. Есть CronJob - которая периодически проверяет состояние VAULT (seal | unseal). Если VAULT = SEAL -> cronJob, берет ключи из локальной директории на Control-plane и делает процедуру UNSEAL
   1. интервал срабатывания +- 5 минут

# Какой итог
1. Vault используется в связке с ESO
2. ESO подключился, скачал нужные секреты в кластер kubernetes
3. Потом Vault перезагрузился и перешел в состояние SEAL
4. на протяжении 5ти минут - ESO не сможет обновлять secrets в k8s-secrets
   1. Это вроде как вообще не проблема