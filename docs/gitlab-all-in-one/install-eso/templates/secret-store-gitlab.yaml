# =============================================================================
# SECRET STORE FOR GITLAB SECRETS
# =============================================================================

apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: {{ .Values.gitlab.secretStoreName }}
  namespace: {{ .Values.namespace }}
spec:
  provider:
    vault:
      server: http://vault.{{ .Values.vault.namespace }}.svc.{{ .Values.dnsDomain }}:8200
      path: {{ .Values.gitlab.kvEnginePath }}
      version: v2
      auth:
        kubernetes:
          mountPath: kubernetes
          role: {{ .Values.gitlab.roleName }}
          serviceAccountRef:
            name: {{ .Values.gitlab.saName }}
