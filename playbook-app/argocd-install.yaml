# =============================================================================
# Install ArgoCD via local Helm charts
# Steps:
#   1. argocd/pre: NetworkPolicies + ESO (ServiceAccount, SecretStore, ExternalSecret)
#   2. argocd/install: ArgoCD manifests + Ingress + default AppProject
#
# Usage (tags):
#   ansible-playbook argocd-install.yaml              # full install
#   ansible-playbook argocd-install.yaml --tags pre   # only pre (NetworkPolicies + ESO)
#   ansible-playbook argocd-install.yaml --tags install
#
# NOTE: Admin password management → argocd-configure.yaml
# =============================================================================

- name: Install ArgoCD via local Helm chart
  hosts: managers
  become: true
  gather_facts: false

  tasks:
    - name: "[argocd-install-pre-check] Validate target"
      include_tasks: tasks/tasks-pre-check.yaml
      vars:
        label_name: "argocd-install-pre-check"
      tags: [always]

    # =========================================================================
    # INIT: Merge ESO integrations and resolve ArgoCD config
    # =========================================================================

    - name: "[argocd-install-init] Merge ESO integrations"
      include_tasks: tasks/tasks-eso-merge.yaml
      tags: [always]

    - name: "[argocd-install-init] Set argocd_eso from merged integration"
      set_fact:
        argocd_eso: "{{ eso_vault_integration_argocd_merged }}"
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    # =========================================================================
    # INIT: Resolve ClusterIssuer + ACME solver pod labels
    # =========================================================================

    - name: "[argocd-install-init] Resolve ArgoCD ClusterIssuer"
      set_fact:
        _argocd_ui_issuer: >-
          {{ cert_manager_cluster_issuers
             | selectattr('name', 'equalto', argocd_cluster_issuer_name)
             | list | first | default(none) }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-install-init] Validate ArgoCD ClusterIssuer exists"
      assert:
        that: _argocd_ui_issuer is not none
        fail_msg: >-
          argocd_cluster_issuer_name='{{ argocd_cluster_issuer_name }}'
          not found in cert_manager_cluster_issuers
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-install-init] Resolve ACME solver by ingressClass"
      set_fact:
        _argocd_ui_solver: >-
          {{ _argocd_ui_issuer.solvers
             | selectattr('ingressClass', 'equalto', argocd_ingress_class_name)
             | list | first | default(none) }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-install-init] Validate ACME solver exists"
      assert:
        that: _argocd_ui_solver is not none
        fail_msg: >-
          No solver with ingressClass='{{ argocd_ingress_class_name }}'
          found in ClusterIssuer '{{ argocd_cluster_issuer_name }}'
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-install-init] Set ACME solver pod labels"
      set_fact:
        argocd_acme_solver_pod_labels: "{{ _argocd_ui_solver.podLabels }}"
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    # =========================================================================
    # STEP 1: ARGOCD/PRE (NetworkPolicies + ESO resources)
    # =========================================================================

    - name: "[argocd-pre] Copy pre chart to remote"
      include_tasks: tasks/tasks-copy-chart.yaml
      vars:
        label_name: "argocd-pre"
        chart_name: "pre"
        chart_local_src: "{{ inventory_dir }}/playbook-app/charts/argocd/pre/"
        chart_remote_dest: "{{ remote_charts_dir }}/argocd/pre"
      tags: [pre]

    - name: "[argocd-pre] Create values file"
      copy:
        dest: "{{ remote_charts_dir }}/argocd/pre/values-override.yaml"
        mode: "0600"
        content: |
          namespace: {{ argocd_namespace }}
          traefik:
            namespace: {{ traefik_namespace }}
          gitlab:
            namespace: {{ gitlab_namespace }}
          clusterDomain: {{ dns_domain }}
          vault:
            namespace: {{ vault_namespace }}
          eso:
            saName: {{ argocd_eso.sa_name }}
            roleName: {{ argocd_eso.role_name }}
            secretStoreName: {{ argocd_eso.secret_store_name }}
            kvEnginePath: {{ argocd_eso.kv_engine_path }}
            isNeedEso: {{ argocd_eso.is_need_eso | default(true) }}
            secrets: {{ argocd_eso.secrets | to_json }}
          esoResourcesConfig:
            secretStoreRefreshInterval: {{ eso_resources_config.secret_store_refresh_interval }}
            externalSecretRefreshInterval: {{ eso_resources_config.external_secret_refresh_interval }}
            externalSecretRefreshPolicy: {{ eso_resources_config.external_secret_refresh_policy }}
          configMaps:
            argocdCm:
              data: {{ argocd_cm_extra | to_json }}
            cmdParamsCm:
              data: {{ argocd_cm_cmd_params_extra | to_json }}
            gpgKeysCm:
              data: {{ argocd_cm_gpg_keys_extra | to_json }}
            notificationsCm:
              data: {{ argocd_cm_notifications_extra | to_json }}
            rbacCm:
              data: {{ argocd_cm_rbac_extra | to_json }}
            sshKnownHostsCm:
              additional: {{ argocd_cm_ssh_known_hosts_extra | to_json }}
            tlsCertsCm:
              data: {{ argocd_cm_tls_certs_extra | to_json }}
          acmeSolverLabels: {{ argocd_acme_solver_pod_labels | to_json }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [pre]

    - name: "[argocd-pre] Install or upgrade via Helm"
      command: >
        helm upgrade --install argocd-pre {{ remote_charts_dir }}/argocd/pre
        --namespace {{ argocd_namespace }}
        --create-namespace
        --values {{ remote_charts_dir }}/argocd/pre/values-override.yaml
        --cleanup-on-fail
        --atomic
        --wait
        --wait-for-jobs
        --timeout {{ argocd_helm_timeout }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [pre]

    # =========================================================================
    # STEP 2: ARGOCD/INSTALL (ArgoCD manifests + Ingress + AppProject)
    # =========================================================================

    - name: "[argocd-install] Copy install chart to remote"
      include_tasks: tasks/tasks-copy-chart.yaml
      vars:
        label_name: "argocd-install"
        chart_name: "install"
        chart_local_src: "{{ inventory_dir }}/playbook-app/charts/argocd/install/"
        chart_remote_dest: "{{ remote_charts_dir }}/argocd/install"
      tags: [install]

    - name: "[argocd-install] Install argocd-crds via kubectl"
      command: kubectl create -f {{ remote_charts_dir }}/argocd/install/crds/crds.yaml
      failed_when: false
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [install]

    - name: "[argocd-install] Wait for CRDs"
      include_tasks: tasks/tasks-wait-crds.yaml
      vars:
        label_name: "argocd-install"
        crds_list:
          - crd/applications.argoproj.io
          - crd/applicationsets.argoproj.io
          - crd/appprojects.argoproj.io
      tags: [install]

    - name: "[argocd-install] Create values file"
      copy:
        dest: "{{ remote_charts_dir }}/argocd/install/values-override.yaml"
        mode: "0600"
        content: |
          namespace: {{ argocd_namespace }}
          ingress:
            uiDomain: {{ argocd_ui_domain }}
            rpcDomain: {{ argocd_rpc_domain }}
            ingressClass: {{ argocd_ingress_class_name }}
            entrypoint: {{ traefik_websecure_entrypoint }}
            clusterIssuer: {{ argocd_cluster_issuer_name }}
            uiSecretName: {{ argocd_ui_https_secret_name }}
            rpcSecretName: {{ argocd_rpc_https_secret_name }}
            vpnOnlyEnabled: {{ argocd_vpn_only_enabled }}
            httpEnabled: {{ argocd_http_enabled }}
          traefik:
            namespace: {{ traefik_namespace }}
          gitlab:
            namespace: {{ gitlab_namespace }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [install]

    - name: "[argocd-install] Install or upgrade argocd via Helm"
      command: >
        helm upgrade --install argocd {{ remote_charts_dir }}/argocd/install
        --namespace {{ argocd_namespace }}
        --create-namespace
        --values {{ remote_charts_dir }}/argocd/install/values-override.yaml
        --cleanup-on-fail
        --skip-crds
        --atomic
        --wait
        --wait-for-jobs
        --timeout {{ argocd_helm_timeout }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [install]

    # =========================================================================
    # VERIFICATION
    # =========================================================================

    - name: "[argocd-install] Wait for rollouts"
      include_tasks: tasks/tasks-wait-rollout.yaml
      vars:
        label_name: "argocd-install"
        rollout_namespace: "{{ argocd_namespace }}"
        rollout_timeout: "{{ argocd_components_rollout_timeout }}"
        rollout_resources:
          - statefulset/argocd-application-controller
          - deployment/argocd-applicationset-controller
          - deployment/argocd-dex-server
          - deployment/argocd-notifications-controller
          - deployment/argocd-redis
          - deployment/argocd-repo-server
          - deployment/argocd-server
      tags: [install]

    # =========================================================================
    # VERIFICATION
    # =========================================================================

    - name: "[argocd-verify] Verify Helm releases"
      include_tasks: tasks/tasks-verify-helm.yaml
      vars:
        helm_namespace: "{{ argocd_namespace }}"
        label_name: "argocd-verify"
      tags: [always]

    - name: "[argocd-verify] Get argocd pods status"
      command: kubectl get pods -n {{ argocd_namespace }} -o wide
      register: argocd_pods
      changed_when: false
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-verify] Print argocd pods status"
      debug:
        msg: "{{ argocd_pods.stdout_lines }}"
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-verify] Verify NetworkPolicies"
      command: kubectl get networkpolicies -n {{ argocd_namespace }}
      register: np_verify
      changed_when: false
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-verify] Show NetworkPolicies"
      debug:
        var: np_verify.stdout_lines
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[argocd-verify] Installation complete"
      debug:
        msg:
          - "argocd-pre + argocd: OK"
          - "Run argocd-configure.yaml to save admin password to Vault."
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]
