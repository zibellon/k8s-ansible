# =============================================================================
# Install External Secrets Operator
# Steps:
#   1. external-secrets/pre: NetworkPolicies
#   2. CRDs (kubectl create -f) + official Helm chart (--skip-crds)
#   3. external-secrets/post: empty
#
# Usage (tags):
#   ansible-playbook external-secrets-install.yaml              # full install
#   ansible-playbook external-secrets-install.yaml --tags pre   # only pre (NetworkPolicies)
#   ansible-playbook external-secrets-install.yaml --tags install
# =============================================================================

---
- name: Install External Secrets Operator
  hosts: managers
  become: true
  gather_facts: false

  tasks:
    - name: "[external-secrets-install-pre-check] Validate target"
      include_tasks: tasks/tasks-pre-check.yaml
      vars:
        label_name: "external-secrets-install-pre-check"
      tags: [always]

    - name: "[external-secrets-install-pre-check] Forbid kube-system namespace"
      include_tasks: tasks/tasks-forbid-kube-system.yaml
      vars:
        label_name: "external-secrets-install-pre-check"
        namespace_value: "{{ external_secrets_namespace }}"
      tags: [always]

    # =========================================================================
    # STEP 1: EXTERNAL-SECRETS/PRE (NetworkPolicies)
    # =========================================================================

    - name: "[external-secrets-pre] Copy chart to server"
      include_tasks: tasks/tasks-copy-chart.yaml
      vars:
        label_name: "external-secrets-pre"
        chart_name: "pre"
        chart_local_src: "{{ inventory_dir }}/playbook-app/charts/external-secrets/pre/"
        chart_remote_dest: "{{ remote_charts_dir }}/external-secrets/pre"
      tags: [pre]

    - name: "[external-secrets-pre] Create values file"
      copy:
        dest: "{{ remote_charts_dir }}/external-secrets/pre/values-override.yaml"
        mode: "0644"
        content: |
          namespace: {{ external_secrets_namespace }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [pre]

    - name: "[external-secrets-pre] Install or upgrade via Helm"
      command: >
        helm upgrade --install external-secrets-pre {{ remote_charts_dir }}/external-secrets/pre
        --namespace {{ external_secrets_namespace }}
        --create-namespace
        --values {{ remote_charts_dir }}/external-secrets/pre/values-override.yaml
        --cleanup-on-fail
        --atomic
        --wait
        --timeout {{ external_secrets_helm_timeout }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [pre]

    # Add Helm repository
    - name: "[external-secrets-install] Add Helm repository"
      include_tasks: tasks/tasks-add-helm-repo.yaml
      vars:
        label_name: "external-secrets-install"
        helm_repo_name: "external-secrets"
        helm_repo_url: "https://charts.external-secrets.io"
      tags: [install]

    - name: "[external-secrets-install] Ensure install directory exists"
      file:
        path: "{{ remote_charts_dir }}/external-secrets/install"
        state: directory
        mode: "0755"
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [install]

    # Create values file for official chart
    - name: "[external-secrets-install] Create values file"
      copy:
        dest: "{{ remote_charts_dir }}/external-secrets/install/values-override.yaml"
        mode: "0644"
        content: |
          image:
            repository: "{{ external_secrets_repository }}"
            tag: "{{ external_secrets_version }}"
          tolerations: {{ external_secrets_tolerations | to_json }}
          nodeSelector: {{ external_secrets_node_selector | to_json }}
          affinity: {{ external_secrets_affinity | to_json }}
          replicaCount: {{ external_secrets_replica_count }}
          resources: {{ external_secrets_resources | to_json }}
          # ---
          webhook:
            image:
              repository: "{{ external_secrets_repository }}"
              tag: "{{ external_secrets_version }}"
            tolerations: {{ external_secrets_webhook_tolerations | to_json }}
            nodeSelector: {{ external_secrets_webhook_node_selector | to_json }}
            affinity: {{ external_secrets_webhook_affinity | to_json }}
            replicaCount: {{ external_secrets_webhook_replica_count }}
            resources: {{ external_secrets_webhook_resources | to_json }}
          # ---
          certController:
            image:
              repository: "{{ external_secrets_repository }}"
              tag: "{{ external_secrets_version }}"
            tolerations: {{ external_secrets_cert_controller_tolerations | to_json }}
            nodeSelector: {{ external_secrets_cert_controller_node_selector | to_json }}
            affinity: {{ external_secrets_cert_controller_affinity | to_json }}
            replicaCount: {{ external_secrets_cert_controller_replica_count }}
            resources: {{ external_secrets_cert_controller_resources | to_json }}
          extraArgs:
            client-burst: "{{ eso_controller_config.client_burst }}"
            concurrent: "{{ eso_controller_config.concurrent }}"
            enable-managed-secrets-caching: "{{ eso_controller_config.enable_managed_secrets_caching | lower }}"
            store-requeue-interval: "{{ eso_controller_config.store_requeue_interval }}"
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [install]

    # Install official Helm chart
    - name: "[external-secrets-install] Install or upgrade via Helm"
      command: >
        helm upgrade --install external-secrets external-secrets/external-secrets
        --namespace {{ external_secrets_namespace }}
        --values {{ remote_charts_dir }}/external-secrets/install/values-override.yaml
        --cleanup-on-fail
        --atomic
        --wait
        --timeout {{ external_secrets_helm_timeout }}
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [install]

    # =========================================================================
    # WAIT FOR CRDS
    # =========================================================================

    - name: "[external-secrets-install] Wait for CRDs"
      include_tasks: tasks/tasks-wait-crds.yaml
      vars:
        label_name: "external-secrets-install"
        crds_list:
          - crd/externalsecrets.external-secrets.io
          - crd/clustersecretstores.external-secrets.io
          - crd/secretstores.external-secrets.io
          - crd/clusterexternalsecrets.external-secrets.io
          - crd/pushsecrets.external-secrets.io
          - crd/clusterpushsecrets.external-secrets.io
          - crd/acraccesstokens.generators.external-secrets.io
          - crd/cloudsmithaccesstokens.generators.external-secrets.io
          - crd/clustergenerators.generators.external-secrets.io
          - crd/ecrauthorizationtokens.generators.external-secrets.io
          - crd/fakes.generators.external-secrets.io
          - crd/gcraccesstokens.generators.external-secrets.io
          - crd/generatorstates.generators.external-secrets.io
          - crd/githubaccesstokens.generators.external-secrets.io
          - crd/grafanas.generators.external-secrets.io
          - crd/mfas.generators.external-secrets.io
          - crd/passwords.generators.external-secrets.io
          - crd/quayaccesstokens.generators.external-secrets.io
          - crd/sshkeys.generators.external-secrets.io
          - crd/stssessiontokens.generators.external-secrets.io
          - crd/uuids.generators.external-secrets.io
          - crd/vaultdynamicsecrets.generators.external-secrets.io
          - crd/webhooks.generators.external-secrets.io

      tags: [install]

    # =========================================================================
    # WAIT FOR DEPLOYMENT
    # =========================================================================

    - name: "[external-secrets-install] Wait for rollouts"
      include_tasks: tasks/tasks-wait-rollout.yaml
      vars:
        label_name: "external-secrets-install"
        rollout_namespace: "{{ external_secrets_namespace }}"
        rollout_timeout: "{{ external_secrets_rollout_timeout }}"
        rollout_resources:
          - deployment/external-secrets
          - deployment/external-secrets-webhook
          - deployment/external-secrets-cert-controller
      tags: [install]

    # =========================================================================
    # VERIFICATION
    # =========================================================================

    - name: "[external-secrets-verify] Verify Helm releases"
      include_tasks: tasks/tasks-verify-helm.yaml
      vars:
        helm_namespace: "{{ external_secrets_namespace }}"
        label_name: "external-secrets-verify"
      tags: [always]

    - name: "[external-secrets-verify] Get external-secrets pods status"
      command: kubectl get pods -n {{ external_secrets_namespace }} -o wide
      register: externalsecrets_pods
      changed_when: false
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[external-secrets-verify] Print external-secrets pods status"
      debug:
        msg: "{{ externalsecrets_pods.stdout_lines }}"
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]

    - name: "[external-secrets-verify] Installation complete"
      debug:
        msg: "external-secrets-pre + external-secrets: OK"
      delegate_to: "{{ master_manager_fact }}"
      run_once: true
      tags: [always]
