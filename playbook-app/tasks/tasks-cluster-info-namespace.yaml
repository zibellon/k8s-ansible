# =============================================================================
# Cluster Info per Namespace — dump K8s state for ONE namespace
# =============================================================================
# Aggregator. For a given namespace, runs all 10 per-resource list tasks
# (pods, certificates, network policies, deployments, statefulsets, ingresses,
# services, TCPs, IngressRoutes, secrets) plus `helm list`. Each underlying
# task delegates to master_manager_fact and prints its output via debug.
#
# Required vars:
#   dto_label_name              — task label prefix (e.g. "cluster-info")
#   dto_namespace_name          — K8s namespace to dump
#
# Requires global vars: master_manager_fact
# =============================================================================

- name: "[{{ dto_label_name }}-{{ dto_namespace_name }}] Assert required dto params are provided"
  assert:
    that:
      - dto_label_name is defined
      - dto_label_name | length > 0
      - dto_namespace_name is defined
      - dto_namespace_name | length > 0
    fail_msg: "tasks-cluster-info-namespace: dto_label_name and dto_namespace_name are required"
  delegate_to: "{{ master_manager_fact }}"
  run_once: true
  tags: [always]

- name: "[{{ dto_label_name }}-{{ dto_namespace_name }}] Set composite label"
  set_fact:
    cluster_info_ns_label: "{{ dto_label_name }}-{{ dto_namespace_name }}"
  delegate_to: "{{ master_manager_fact }}"
  run_once: true
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List pods"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-pods.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_pods_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List certificates"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-certificate.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_certificate_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List network policies"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-network-policy.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_np_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List deployments"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-deployment.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_deployment_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List statefulsets"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-statefulset.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_statefulset_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List ingresses"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-ingress.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_ingress_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List services"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-service.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_service_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List TCPs"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-tcp.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_tcp_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List IngressRoutes"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-ingress-route.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_ingress_route_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List secrets"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-secret.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_secret_namespace: "{{ dto_namespace_name }}"
  tags: [always]

- name: "[{{ cluster_info_ns_label }}] List Helm releases"
  include_tasks: "{{ project_root }}/playbook-app/tasks/tasks-k8s-list-helm.yaml"
  vars:
    dto_label_name: "{{ cluster_info_ns_label }}"
    dto_helm_namespace: "{{ dto_namespace_name }}"
  tags: [always]
