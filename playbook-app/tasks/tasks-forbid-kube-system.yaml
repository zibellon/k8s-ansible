# =============================================================================
# Forbid kube-system namespace
# =============================================================================
# Asserts that the given namespace is not 'kube-system'.
#
# Params:
#   - label_name:      Label prefix for task names (e.g. "cilium-install-pre-check")
#   - dto_target_namespace: The namespace value to validate
#
# Requires fact: master_manager_fact (set by tasks-pre-check.yaml)
# =============================================================================

- name: "[{{ label_name }}] Assert all dto params are provided"
  assert:
    that:
      - label_name is defined
      - label_name | length > 0
      - dto_target_namespace is defined
      - dto_target_namespace | length > 0
    fail_msg: "tasks-forbid-kube-system: label_name and dto_target_namespace are required"
  run_once: true
  tags: [always]

- name: "[{{ label_name }}] Forbid kube-system namespace"
  assert:
    that: dto_target_namespace != "kube-system"
    fail_msg: "{{ label_name }}: namespace must not be 'kube-system'. Got: '{{ dto_target_namespace }}'"
  delegate_to: "{{ master_manager_fact }}"
  run_once: true
  tags: [always]
