# Cilium namespace
namespace: "cilium"

# Traefik namespace for NetworkPolicy egress rules
traefik:
  namespace: ""

# ACME solver pod labels (from resolved ClusterIssuer solver)
acmeSolverLabels: {}
