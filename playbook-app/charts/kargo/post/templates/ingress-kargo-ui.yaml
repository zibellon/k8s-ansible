# =============================================================================
# KARGO UI INGRESS (Traefik)
#   enabled    → рендерить Ingress
#   tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls (TLS у Cloudflare)
# =============================================================================
{{- if .Values.uiIngressConfig.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kargo-ui
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.uiIngressConfig.ingressClass | quote }}
    {{- if .Values.uiIngressConfig.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.uiIngressConfig.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.uiIngressConfig.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.uiIngressConfig.ingressClass }}
  {{- if and .Values.uiIngressConfig.tlsEnabled .Values.uiIngressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.uiIngressConfig.domain }}
      secretName: {{ .Values.uiIngressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.uiIngressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.apiService.name }}
                port:
                  number: {{ .Values.apiService.port }}
{{- end }}
