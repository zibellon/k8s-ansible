---
# Namespace'ы кластера. Элемент несёт полную спецификацию объекта:
# {name, labels, annotations}. Шаблон рендерит его как есть, ничего
# не вычисляя и не достраивая. labels/annotations опциональны:
# пустые/отсутствующие блоки не рендерятся.
namespaces: []

# Custom K8s objects to render (operator-side, default empty)
extraObjects: []
