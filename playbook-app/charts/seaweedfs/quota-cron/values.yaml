---
# Namespace
namespace: "seaweedfs"

# CronJob schedule (cron expression)
schedule: "*/5 * * * *"

# Image (must match upstream SeaweedFS chart version для protocol compatibility)
image: "chrislusf/seaweedfs:4.29"

# Custom K8s objects to render (operator-side, default empty)
extraObjects: []
