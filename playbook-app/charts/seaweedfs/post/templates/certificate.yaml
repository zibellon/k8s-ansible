# =============================================================================
# CERTIFICATES FOR SEAWEEDFS (cert-manager)
#   Рендерится только если issuer.enabled И per-config certificate.enabled.
#   issuer выключен → оба Certificate молча пропускаются (без ошибки).
# =============================================================================
{{- if .Values.issuer.enabled }}
{{- if .Values.s3IngressConfig.certificate.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.s3IngressConfig.tlsSecretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.s3IngressConfig.tlsSecretName }}
  dnsNames:
    - "{{ .Values.s3IngressConfig.domain }}"
  issuerRef:
    name: {{ .Values.issuer.body.name }}
    kind: Issuer
{{- end }}
{{- if .Values.adminUiIngressConfig.certificate.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.adminUiIngressConfig.tlsSecretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.adminUiIngressConfig.tlsSecretName }}
  dnsNames:
    - "{{ .Values.adminUiIngressConfig.domain }}"
  issuerRef:
    name: {{ .Values.issuer.body.name }}
    kind: Issuer
{{- end }}
{{- end }}
