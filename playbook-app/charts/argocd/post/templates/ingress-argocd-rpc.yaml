# =============================================================================
# INGRESSROUTE FOR ARGOCD RPC / gRPC (Traefik)
#   ingress.enabled    → рендерить IngressRoute
#   ingress.tlsEnabled → websecure entrypoint + tls-блок; иначе web, без tls
# Matches gRPC requests by Content-Type header. Higher priority than UI route.
# =============================================================================
{{- if .Values.rpcIngressConfig.ingress.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-rpc
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: "{{ .Values.rpcIngressConfig.ingress.ingressClass }}"
spec:
  entryPoints:
    {{- if .Values.rpcIngressConfig.ingress.tlsEnabled }}
    - {{ .Values.traefik.entryPoints.websecure }}
    {{- else }}
    - {{ .Values.traefik.entryPoints.web }}
    {{- end }}
  routes:
    - kind: Rule
      match: Host(`{{ .Values.rpcIngressConfig.domain }}`) && Header(`Content-Type`, `application/grpc`)
      priority: 11
      {{- if .Values.rpcIngressConfig.ingress.vpnOnlyEnabled }}
      middlewares:
        {{- range .Values.rpcIngressConfig.ingress.vpnIngressRouteMiddlewares }}
        - name: {{ .name }}
          namespace: {{ .namespace }}
        {{- end }}
      {{- end }}
      services:
        - name: argocd-server
          port: 80
          scheme: h2c
  {{- if and .Values.rpcIngressConfig.ingress.tlsEnabled .Values.rpcIngressConfig.tlsSecretName }}
  tls:
    secretName: {{ .Values.rpcIngressConfig.tlsSecretName }}
  {{- end }}
{{- end }}
