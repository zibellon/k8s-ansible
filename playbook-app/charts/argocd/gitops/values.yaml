---
# Namespace
namespace: "argocd"

# AppProjects — каждый принимает ПОЛНЫЙ spec (operator-provided); template оборачивает metadata.
appProjects: []
# - name: "prj-root"
#   spec:
#     sourceRepos: ["*"]
#     destinations:
#       - name: "*"
#         namespace: "*"
#         server: "*"
#     clusterResourceWhitelist:
#       - group: "*"
#         kind: "*"

# Applications — каждый принимает ПОЛНЫЙ spec; template добавляет namespace + (опционально) finalizers.
applications: []
# - name: "app-of-apps-root"
#   finalizers:                                      # optional; пустой/отсутствует → не добавляется
#     - resources-finalizer.argocd.argoproj.io/background
#   spec:
#     project: "prj-root"
#     source:
#       repoURL: ""
#       targetRevision: "master"
#       path: "argocd-system"
#     destination:
#       server: "https://kubernetes.default.svc"
#     syncPolicy:
#       automated: {prune: true, selfHeal: true, allowEmpty: true}
#       retry: {limit: 5, backoff: {duration: "20s", factor: 1, maxDuration: "30s"}}

# Custom K8s objects to render (operator-side, default empty)
extraObjects: []
