# Traefik Install

This directory is a placeholder. Traefik is installed from the official Helm chart:

CRDs are installed automatically with the official chart.

See [traefik-install.yaml](../../../traefik-install.yaml) for the full installation playbook.
