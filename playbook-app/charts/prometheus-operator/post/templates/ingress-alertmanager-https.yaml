# =============================================================================
# INGRESS FOR ALERTMANAGER UI — HTTPS (Traefik)
# =============================================================================

{{- if .Values.alertmanagerIngress.httpsEnable }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.alertmanagerIngress.secretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.alertmanagerIngress.secretName }}
  dnsNames:
    - {{ .Values.alertmanagerIngress.domain }}
  issuerRef:
    name: {{ .Values.clusterIssuer }}
    kind: ClusterIssuer

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-alertmanager-https
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: "{{ .Values.ingressClass }}"
    traefik.ingress.kubernetes.io/router.entrypoints: "{{ .Values.entryPoints.websecure }}"
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- if .Values.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.traefik.namespace }}-vpn-only@kubernetescrd"
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingressClass }}
  tls:
    - hosts:
        - {{ .Values.alertmanagerIngress.domain }}
      secretName: {{ .Values.alertmanagerIngress.secretName }}
  rules:
    - host: {{ .Values.alertmanagerIngress.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: alertmanager-instance
                port:
                  number: 9093
{{- end }}
