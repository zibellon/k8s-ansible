# =============================================================================
# EXTERNAL SECRETS FOR GITLAB V2 (from Vault)
# =============================================================================

{{- range $secret := .Values.gitlabV2Eso.secrets }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ $secret.external_secret_name }}
  namespace: {{ $.Values.namespace }}
spec:
  refreshInterval: {{ $secret.refresh_interval }}
  secretStoreRef:
    kind: SecretStore
    name: {{ $.Values.gitlabV2Eso.secretStoreName }}
  target:
    name: {{ $secret.target_secret_name }}
  dataFrom:
    - extract:
        key: {{ $.Values.gitlabV2Eso.kvEnginePath }}/data{{ $secret.vault_path }}
{{- end }}
