---
# PostgreSQL configuration
namespace: gitlab

image: postgres:16-alpine

# Storage
storageClass: lh-major-multi-best-effort
storageSize: 20Gi

# Credentials secret (from ESO)
credentialsSecretName: ""

# Custom K8s objects to render (operator-side, default empty)
extraObjects: []
