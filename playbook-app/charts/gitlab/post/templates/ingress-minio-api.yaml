# =============================================================================
# INGRESS FOR MINIO API (Traefik)
#   ingress.enabled    → рендерить Ingress
#   ingress.tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls
# =============================================================================
{{- if .Values.minioApiIngressConfig.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio-api
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.minioApiIngressConfig.ingress.ingressClass | quote }}
    {{- if .Values.minioApiIngressConfig.ingress.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.minioApiIngressConfig.ingress.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.minioApiIngressConfig.ingress.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.minioApiIngressConfig.ingress.ingressClass }}
  {{- if and .Values.minioApiIngressConfig.ingress.tlsEnabled .Values.minioApiIngressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.minioApiIngressConfig.domain }}
      secretName: {{ .Values.minioApiIngressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.minioApiIngressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gitlab-minio
                port:
                  number: 9000
{{- end }}
