# =============================================================================
# INGRESS FOR GITLAB PAGES (Traefik)
#   enabled    → рендерить Ingress
#   tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls
# =============================================================================
{{- if .Values.pagesIngressConfig.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gitlab-pages
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.pagesIngressConfig.ingressClass | quote }}
    {{- if .Values.pagesIngressConfig.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.pagesIngressConfig.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.pagesIngressConfig.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.pagesIngressConfig.ingressClass }}
  {{- if and .Values.pagesIngressConfig.tlsEnabled .Values.pagesIngressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.pagesIngressConfig.domain }}
      secretName: {{ .Values.pagesIngressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.pagesIngressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gitlab-gitlab-pages
                port:
                  number: 8090
{{- end }}
