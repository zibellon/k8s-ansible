# =============================================================================
# INGRESS FOR GITLAB PAGES (Traefik)
#   ingress.enabled    → рендерить Ingress
#   ingress.tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls
# =============================================================================
{{- if .Values.pagesIngressConfig.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gitlab-pages
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.pagesIngressConfig.ingress.ingressClass | quote }}
    {{- if .Values.pagesIngressConfig.ingress.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.pagesIngressConfig.ingress.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.pagesIngressConfig.ingress.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.pagesIngressConfig.ingress.ingressClass }}
  {{- if and .Values.pagesIngressConfig.ingress.tlsEnabled .Values.pagesIngressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.pagesIngressConfig.domain }}
      secretName: {{ .Values.pagesIngressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.pagesIngressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gitlab-gitlab-pages
                port:
                  number: 8090
{{- end }}
