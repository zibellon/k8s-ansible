# =============================================================================
# CERTIFICATES FOR GITLAB (cert-manager)
#   Каждый Certificate рендерится только если issuer.enabled И
#   <unit>IngressConfig.certificate.enabled. issuer выключен → Certificate
#   молча пропускается (без ошибки).
# =============================================================================
{{- if and .Values.issuer.enabled .Values.uiIngressConfig.certificate.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.uiIngressConfig.tlsSecretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.uiIngressConfig.tlsSecretName }}
  dnsNames:
    - {{ .Values.uiIngressConfig.domain }}
  issuerRef:
    name: {{ .Values.issuer.body.name }}
    kind: Issuer
{{- end }}
{{- if and .Values.issuer.enabled .Values.registryIngressConfig.certificate.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.registryIngressConfig.tlsSecretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.registryIngressConfig.tlsSecretName }}
  dnsNames:
    - {{ .Values.registryIngressConfig.domain }}
  issuerRef:
    name: {{ .Values.issuer.body.name }}
    kind: Issuer
{{- end }}
{{- if and .Values.issuer.enabled .Values.pagesIngressConfig.certificate.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.pagesIngressConfig.tlsSecretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.pagesIngressConfig.tlsSecretName }}
  dnsNames:
    - {{ .Values.pagesIngressConfig.domain }}
  issuerRef:
    name: {{ .Values.issuer.body.name }}
    kind: Issuer
{{- end }}
{{- if and .Values.issuer.enabled .Values.minioApiIngressConfig.certificate.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.minioApiIngressConfig.tlsSecretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.minioApiIngressConfig.tlsSecretName }}
  dnsNames:
    - {{ .Values.minioApiIngressConfig.domain }}
  issuerRef:
    name: {{ .Values.issuer.body.name }}
    kind: Issuer
{{- end }}
{{- if and .Values.issuer.enabled .Values.minioConsoleIngressConfig.certificate.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.minioConsoleIngressConfig.tlsSecretName }}
  namespace: {{ .Values.namespace }}
spec:
  secretName: {{ .Values.minioConsoleIngressConfig.tlsSecretName }}
  dnsNames:
    - {{ .Values.minioConsoleIngressConfig.domain }}
  issuerRef:
    name: {{ .Values.issuer.body.name }}
    kind: Issuer
{{- end }}
