---
# MinIO configuration
namespace: gitlab

image: minio/minio:latest

# Storage
storageClass: lh-major-single-best-effort
storageSize: 50Gi

# Root credentials secret (from ESO)
rootSecretName: ""

# Custom K8s objects to render (operator-side, default empty)
extraObjects: []
