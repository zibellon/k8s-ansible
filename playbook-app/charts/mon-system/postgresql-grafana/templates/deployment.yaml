apiVersion: apps/v1
kind: Deployment
metadata:
  name: mon-system-postgresql-grafana
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: mon-system-postgresql-grafana
    app.kubernetes.io/instance: mon-system-postgresql-grafana-{{ .Values.namespace }}
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: mon-system
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: mon-system-postgresql-grafana
      app.kubernetes.io/instance: mon-system-postgresql-grafana-{{ .Values.namespace }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mon-system-postgresql-grafana
        app.kubernetes.io/instance: mon-system-postgresql-grafana-{{ .Values.namespace }}
        app.kubernetes.io/component: database
        app.kubernetes.io/part-of: mon-system
        app.kubernetes.io/managed-by: Helm
    spec:
      automountServiceAccountToken: false
      tolerations: {{ .Values.tolerations | toJson }}
      nodeSelector: {{ .Values.nodeSelector | toJson }}
      affinity: {{ .Values.affinity | toJson }}
      containers:
        - name: postgresql
          image: {{ .Values.image }}
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.credentialsSecretName }}
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.credentialsSecretName }}
                  key: password
            - name: POSTGRES_DB
              value: {{ .Values.databaseName | quote }}
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgresql
              containerPort: {{ .Values.port }}
              protocol: TCP
          resources: {{ .Values.resources | toJson }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: mon-system-postgresql-grafana
