# =============================================================================
# INGRESS FOR PROMETHEUS UI (Traefik)
#   prometheus_operator.enabled + prometheus.enabled → workload-enable gate (composite, внешний)
#   enabled    → рендерить Ingress
#   tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls
# =============================================================================
{{- if and .Values.prometheus_operator.enabled .Values.prometheus.enabled }}
{{- if .Values.prometheusIngressConfig.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.prometheusIngressConfig.ingressClass | quote }}
    {{- if .Values.prometheusIngressConfig.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.prometheusIngressConfig.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.prometheusIngressConfig.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.prometheusIngressConfig.ingressClass }}
  {{- if and .Values.prometheusIngressConfig.tlsEnabled .Values.prometheusIngressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.prometheusIngressConfig.domain }}
      secretName: {{ .Values.prometheusIngressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.prometheusIngressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prometheus-instance
                port:
                  number: {{ .Values.prometheus.port }}
{{- end }}
{{- end }}
