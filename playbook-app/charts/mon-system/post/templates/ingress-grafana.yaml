# =============================================================================
# INGRESS FOR GRAFANA (Traefik)
#   grafana.enabled    → workload-enable gate (composite, внешний)
#   enabled    → рендерить Ingress
#   tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls
# =============================================================================
{{- if .Values.grafana.enabled }}
{{- if .Values.grafanaIngressConfig.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ui
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.grafanaIngressConfig.ingressClass | quote }}
    {{- if .Values.grafanaIngressConfig.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.grafanaIngressConfig.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.grafanaIngressConfig.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.grafanaIngressConfig.ingressClass }}
  {{- if and .Values.grafanaIngressConfig.tlsEnabled .Values.grafanaIngressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.grafanaIngressConfig.domain }}
      secretName: {{ .Values.grafanaIngressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.grafanaIngressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: {{ .Values.grafana.port }}
{{- end }}
{{- end }}
