# =============================================================================
# INGRESS FOR ALERTMANAGER UI (Traefik)
#   prometheus_operator.enabled + alertmanager.enabled → workload-enable gate (composite, внешний)
#   ingress.enabled    → рендерить Ingress
#   ingress.tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls
# =============================================================================
{{- if and .Values.prometheus_operator.enabled .Values.alertmanager.enabled }}
{{- if .Values.alertmanagerIngressConfig.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alertmanager
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.alertmanagerIngressConfig.ingress.ingressClass | quote }}
    {{- if .Values.alertmanagerIngressConfig.ingress.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.alertmanagerIngressConfig.ingress.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.alertmanagerIngressConfig.ingress.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.alertmanagerIngressConfig.ingress.ingressClass }}
  {{- if and .Values.alertmanagerIngressConfig.ingress.tlsEnabled .Values.alertmanagerIngressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.alertmanagerIngressConfig.domain }}
      secretName: {{ .Values.alertmanagerIngressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.alertmanagerIngressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: alertmanager-instance
                port:
                  number: {{ .Values.alertmanager.port }}
{{- end }}
{{- end }}
