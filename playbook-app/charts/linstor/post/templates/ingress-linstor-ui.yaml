# =============================================================================
# LINSTOR CONTROLLER GUI INGRESS
#   GUI отдаётся самим linstor-controller (REST API + /ui/) на порту 3370.
#   ingress.enabled    → рендерить Ingress
#   ingress.tlsEnabled → websecure entrypoint + router.tls + tls-блок; иначе web, без tls
# =============================================================================
{{- if .Values.ingressConfig.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: linstor-ui
  namespace: {{ .Values.namespace }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.ingressConfig.ingress.ingressClass | quote }}
    {{- if .Values.ingressConfig.ingress.tlsEnabled }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.websecure | quote }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- else }}
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.traefik.entryPoints.web | quote }}
    {{- end }}
    {{- if .Values.ingressConfig.ingress.vpnOnlyEnabled }}
    traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.ingressConfig.ingress.vpnIngressMiddlewares }}"
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingressConfig.ingress.ingressClass }}
  {{- if and .Values.ingressConfig.ingress.tlsEnabled .Values.ingressConfig.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.ingressConfig.domain }}
      secretName: {{ .Values.ingressConfig.tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingressConfig.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: linstor-controller
                port:
                  number: 3370
{{- end }}
