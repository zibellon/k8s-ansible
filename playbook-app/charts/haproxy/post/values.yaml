namespace: "haproxy-lb"
ingressClass: "haproxy-lb"

# Global config — overridden via hosts-vars haproxy_global_config
global: {}

# Defaults config — overridden via hosts-vars haproxy_defaults_config
defaults: {}
