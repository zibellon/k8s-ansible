---
# ClusterIssuers — массив raw ClusterIssuer specs ({name, spec}); поддерживает несколько.
# В проде переопределяется cert_manager_post_helm_values.clusterIssuers (= cert_manager_cluster_issuers).
clusterIssuers: []

# Custom K8s objects to render (operator-side, default empty)
extraObjects: []
