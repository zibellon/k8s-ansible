# =============================================================================
# Longhorn выключен: в кластере используется LINSTOR. Два storage-стека
# параллельно не ставятся — выбирается ровно один. Файл нужен, чтобы выбор был
# явным, а не следствием дефолта.
# =============================================================================
all:
  vars:
    longhorn_enabled: false
