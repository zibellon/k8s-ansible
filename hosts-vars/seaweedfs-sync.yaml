all:
  vars:
    # =============================================================================
    # SeaweedFS Sync — declarative source-of-truth для identities, buckets, policies
    # =============================================================================
    # Этот файл потребляется:
    #   - playbook-app/seaweedfs-install.yaml — bootstrap admin identity (читает
    #     seaweedfs_identities entry где actions=['Admin']).
    #   - playbook-app/seaweedfs-install.yaml — sync через --tags user-sync/bucket-sync/bucket-policy-sync.
    #
    # Архитектура (см. .claude/rules/components.md §17.5):
    #   Layer 1: Identities → Vault path /seaweedfs/s3-config/all (combined JSON)
    #            → ESO template → K8s Secret seaweedfs-s3-identities → mounted в
    #            S3 pods через existingConfigSecret.
    #   Layer 2: Buckets + quotas + AWS IAM policies → filer Postgres metadata
    #            (weed shell для buckets/quotas + CronJob enforce; aws s3api для
    #            policies). Policies — per-bucket field в SECTION 2 schema.
    #   Layer 3: Identity credentials distribution → произвольные дополнительные
    #            Vault paths (через identity.extra_vault_paths, SECTION 1 schema),
    #            ConfigMap-tracked diff sync.
    #
    # base arrays — committed (admin + anonymous identities в seaweedfs_identities).
    # _extra arrays — для hosts-vars-override/, merged at runtime через `+ (var | default([]))`.
    # =============================================================================

    # ------
    # S3 identity credentials generation (consumed by seaweedfs_user_sync_full)
    # ------
    # AK/SK длины и charset'ы для generation новых identity credentials в Vault
    # combined JSON. Используются ТОЛЬКО при CREATE новой identity (in target,
    # not in Vault yet). Для existing identities — preserve existing creds.
    seaweedfs_sync_access_key_length: 20
    seaweedfs_sync_secret_key_length: 40
    seaweedfs_sync_access_key_charset: "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
    seaweedfs_sync_secret_key_charset: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789"

    # ------
    # SECTION 1: Identities
    # ------
    # SeaweedFS S3 IAM identities — admin + per-user named principals + anonymous.
    # Sync (--tags user-sync) приводит Vault combined JSON к target state:
    #   CREATE (in target, not in Vault): generate fresh access_key (20 chars
    #     uppercase+digits) + secret_key (40 chars letters+digits); store в Vault
    #     combined JSON.
    #   UPDATE (in both, actions differ): keep existing keys, refresh actions.
    #   DELETE (in Vault, not in target): remove from combined JSON.
    #
    # Schema per identity:
    #   name:    SeaweedFS identity name. Используется в bucket policies как
    #            Principal: "arn:aws:iam::*:user/<name>" (wildcard accountID).
    #            Special name 'anonymous' для public-read (Principal: '*' в bucket
    #            policy совпадёт с anonymous identity — special name required
    #            in static JSON; см. buildPrincipalARN auth_credentials.go:1640).
    #   actions: SeaweedFS-internal actions list.
    #            - admin:  ["Admin"] → bypass identity check (isAdmin()).
    #            - users:  [] → "policy-only principals" (auth_credentials.go:1481),
    #                        доступ ТОЛЬКО через bucket policy Allow (по design,
    #                        не workaround).
    #            - anonymous: [] (same).
    #   extra_vault_paths: OPTIONAL list of full Vault paths (с mount engine prefix,
    #            например "eso-secret/gitlab/s3-storage" или "secret/team-alpha/creds").
    #            Sync (--tags identity-distribute) дополнительно доставляет identity
    #            credentials в каждый указанный путь поверх mandatory combined JSON.
    #            Fixed Vault field keys (HARD-CODED, изменить нельзя):
    #              username   — identity name (например "alice")
    #              accessKey  — из combined JSON identities[].credentials[0].accessKey
    #              secretKey  — из combined JSON identities[].credentials[0].secretKey
    #            Distribute идёт через ConfigMap state seaweedfs-sync-identity-distributions-state
    #            в namespace seaweedfs (diff-based: removed paths → vault-delete;
    #            target paths → vault-put full-replace).
    #            Anonymous identity с непустым extra_vault_paths → fail на sync
    #            (нет creds для distribute).
    #            Если поле отсутствует или [] — distribute для этой identity не выполняется.
    #
    # Admin safety guard в sync playbook: warning если в target identities нет
    # ни одной entry с actions=['Admin'] (защита от accidental lockout).
    # ------
    # Admin identity — отдельная переменная для устранения hardcoded 'admin' в коде.
    # Operator может переопределить name через hosts-vars-override/seaweedfs-sync.yaml.
    seaweedfs_identity_admin:
      name: admin
      actions: ["Admin"]

    seaweedfs_identities:
      - "{{ seaweedfs_identity_admin }}"
      - name: anonymous
        actions: []
    seaweedfs_identities_extra: []
    # Пример operator extension (для hosts-vars-override/seaweedfs-sync.yaml):
    # seaweedfs_identities_extra:
    #   - name: alice
    #     actions: []
    #     extra_vault_paths:
    #       - "eso-secret/gitlab/s3-storage"
    #       - "secret/team-alpha/s3-creds"
    #   - name: bob
    #     actions: []
    #     # без extra_vault_paths — distribute не выполняется для bob

    # ------
    # --- Pre-built opt-in для GitLab + GitLab-Runner identities ---
    # Если используете SeaweedFS как S3 backend для GitLab / gitlab-runner — раскомментируйте
    # этот блок в своём hosts-vars-override/seaweedfs-sync.yaml. Это создаст identities
    # 'gitlab' + 'gitlab-runner', distribute их creds в Vault через identity-distribute
    # в paths из gitlab/gitlab-runner inventory ESO secrets (variable references — single
    # source of truth, не hardcode). Альтернатива: внешнее S3 (cloud) — operator вручную
    # делает 'vault kv put' для тех же resolved путей.
    # seaweedfs_identities_extra:
    #   - name: gitlab
    #     actions: []   # policy-only principal (доступ только через bucket policy Allow)
    #     extra_vault_paths:
    #       - "{{ eso_vault_integration_gitlab.kv_engine_path }}{{ gitlab_secret_s3_creds.vault_path }}"
    #   - name: gitlab-runner
    #     actions: []
    #     extra_vault_paths:
    #       - "{{ eso_vault_integration_gitlab_runner.kv_engine_path }}{{ gitlab_runner_secret_s3_creds.vault_path }}"
    # ------
    # --- Pre-built opt-in для Loki (mon-system) identity ---
    # Если используете SeaweedFS как S3 backend для Loki — раскомментируйте этот блок
    # (объедините с gitlab-блоком выше в ОДИН seaweedfs_identities_extra, если нужны оба).
    # Создаёт identity 'loki', distribute её creds в Vault path из mon-system ESO secret
    # (variable reference — single source of truth). Альтернатива: внешний S3 — operator
    # вручную делает 'vault kv put' для того же resolved пути.
    # seaweedfs_identities_extra:
    #   - name: loki
    #     actions: []   # policy-only principal (доступ только через bucket policy Allow)
    #     extra_vault_paths:
    #       - "{{ eso_vault_integration_mon_system.kv_engine_path }}{{ mon_system_secret_loki_s3_creds.vault_path }}"

    # ------
    # SECTION 2: Buckets + quotas + policies
    # ------
    # S3 buckets + per-bucket SeaweedFS-native quotas + optional AWS IAM policy.
    # Sync (--tags bucket-sync) применяет diff vs K8s ConfigMap state
    # 'seaweedfs-sync-buckets-state' (хранит target snapshot — [{name, quota, policy}, ...]).
    #
    # Sync order (6 фаз):
    #   1. DELETE стейл buckets (in state, not in target): weed shell
    #      's3.bucket.delete -name=<n>' — hard delete via master (CollectionDelete
    #      sweeps volumes/objects; policies swept automatically). См. NOTE ниже.
    #   2. DELETE стейл policies у kept buckets (in state with policy, in target
    #      without): aws s3api delete-bucket-policy --bucket <n>.
    #   3. APPLY policies у kept buckets (target has policy — всегда put,
    #      idempotent): aws s3api put-bucket-policy --bucket <n> --policy '<JSON>'.
    #   4. CREATE new buckets (in target, not in state): weed shell
    #      's3.bucket.create -name=<n> -owner={{ seaweedfs_identity_admin.name }}'; затем apply policy (если есть).
    #   5. APPLY quotas для всех target buckets с quota.defined (новых и kept):
    #      weed shell 's3.bucket.quota -name=<n> -op=set|disable -sizeMB=<X>'.
    #   6. UPDATE ConfigMap state с новым target snapshot.
    #
    # NOTE про bucket delete: в SeaweedFS shell command 's3.bucket.delete' нет
    # флага -force. Команда сама по себе делает hard delete — CollectionDelete
    # напрямую через master (sweeps все volumes/objects bucket'а) + filer_pb.Remove
    # (sweeps bucket record). Bucket с объектами удаляется без жалоб.
    # Единственное препятствие — Object Lock с locked objects (s3_objectlock check).
    #
    # Quota enforcement: нативный в SeaweedFS 4.31+ — s3-gateway сам переключает bucket
    # read-only в обе стороны (~раз в минуту, leader-locked на одной из реплик), без
    # внешнего крона. Источник: weed/s3api/bucket_size_metrics.go (enforceBucketQuotas).
    # bucket-sync Phase E только ЗАДАЁТ квоты (s3.bucket.quota -op=set); энфорсит gateway.
    #
    # Schema per bucket:
    #   name:        bucket name (DNS-compatible, lower-case) — required.
    #   rack:        OPTIONAL string — preferred volume tier (SeaweedFS rack axis).
    #                Допустимые = rack-метки volume-групп из hosts-vars/seaweedfs.yaml
    #                (по конвенции 'managers-1' / 'workers-1'). SOFT preference: при заполнении
    #                тира SeaweedFS спилит на другой (altRequest fallback). Пусто = без
    #                преференса (weighted-by-free-space). IMMUTABLE post-creation.
    #   dataCenter:  OPTIONAL string — preferred DC (SeaweedFS dataCenter axis). Сейчас
    #                кластер одно-DC ('dc-1'). SOFT preference. IMMUTABLE post-creation.
    #   replication: SeaweedFS replication code — REQUIRED 3-digit string '^[0-9]{3}$'.
    #                Format XYZ: X=other DCs, Y=other racks (same DC), Z=other servers (same rack).
    #                Total copies = 1 + sum digits. Examples: '000' (no rep), '001' (+1 same rack),
    #                '100' (+1 other DC), '205' (8 total copies). Same fs.configure command (combined).
    #                ВНИМАНИЕ при rack-преференсе: используй формат 00Z (реплики в том же
    #                тире, e.g. '001'), НЕ 0Y0 ('010') — иначе копия форсится в другой тир,
    #                ломая rack-преференс. Лимиты: workers до '004' (5 нод), managers до '002' (3).
    #   IMPORTANT — replication, rack, dataCenter immutable post-creation:
    #                Change post-create технически работает (fs.configure applies к новым writes),
    #                НО existing data остаётся в old configuration → data fragmentation.
    #                Sync code FAIL-FAST если ЛЮБОЕ из immutable полей changed vs state:
    #                assert после compute calls, до Phase A → sync aborts, cluster intact.
    #                To resolve: revert inventory к state values OR migrate data manually + align.
    #   quota:       OPTIONAL {enabled, size}.
    #     enabled: bool — true → apply size limit, false → disable (keep config).
    #     size:    human-readable размер (e.g. "100GiB", "5TiB") — sync конвертирует
    #              в MiB для weed shell.
    #   policy:      OPTIONAL AWS IAM policy document {Version, Statement: [...]} (один на bucket).
    #     Storage: filer Postgres metadata (entry.Extended["s3-bucket-policy"] —
    #     persistent через pod restart automatically).
    #     Если поле отсутствует — у bucket нет policy.
    #
    # CRITICAL — SeaweedFS 4.29 non-AWS policy deviations:
    #
    #   1. Principal — ТОЛЬКО flat string или array, НЕ AWS dict-form
    #      `{"AWS": "..."}` (policy_engine/types.go:55-77 — UnmarshalJSON
    #      принимает только string или []string).
    #         OK:    Principal: "arn:aws:iam::*:user/alice"
    #         OK:    Principal: ["arn:aws:iam::*:user/alice", "arn:aws:iam::*:user/bob"]
    #         OK:    Principal: "*"     # anonymous (требует anonymous identity)
    #         FAIL:  Principal: {AWS: "arn:aws:iam::*:user/alice"}
    #      Sync task валидирует — отклоняет dict-form с явным сообщением.
    #
    #   2. Resource — должен ссылаться на ЭТОТ bucket (cross-bucket policy запрещён;
    #      s3api_bucket_policy_handlers.go:340-366):
    #         "arn:aws:s3:::<bucket>"    — на сам bucket (s3:ListBucket, s3:GetBucketLocation).
    #         "arn:aws:s3:::<bucket>/*"  — на objects (s3:GetObject, s3:PutObject, ...).
    #
    #   3. Explicit Deny statement блокирует и admin (auth_credentials.go:1487).
    #      Использовать ТОЛЬКО Allow statements; rely on default-deny для остального.
    #
    #   4. Account ID в Principal ARN — wildcard '*' (no per-user account binding).
    # ------
    seaweedfs_sync_buckets: []
    seaweedfs_sync_buckets_extra: []
    # Пример operator extension (для hosts-vars-override/seaweedfs-sync.yaml):
    # seaweedfs_sync_buckets_extra:
    #   - name: bucket-1
    #     replication: "001"   # +1 копия в том же тире
    #     rack: "workers-1"      # preferred tier (soft), IMMUTABLE
    #     dataCenter: "dc-1"    # preferred DC (soft), IMMUTABLE
    #     quota:
    #       enabled: true
    #       size: "100GiB"

    # ------
    # --- Pre-built opt-in для GitLab + GitLab-Runner buckets ---
    # Если используете SeaweedFS как S3 backend для GitLab / gitlab-runner — раскомментируйте
    # этот блок в своём hosts-vars-override/seaweedfs-sync.yaml. 6 buckets с full-access
    # policies (5 для GitLab user, 1 для gitlab-runner user). replication='001' (+1 копия
    # в same rack — measure baseline durability; для более высокой надёжности используйте
    # '100' cross-DC или '101' rack+server).
    # Альтернатива: внешнее S3 — operator сам создаёт buckets вне Ansible.
    # seaweedfs_sync_buckets_extra:
    #   # --- GitLab buckets (5) — owned by 'gitlab' user ---
    #   - name: gitlab-registry
    #     replication: "001"
    #     rack: "workers-1"
    #     dataCenter: "dc-1"
    #     policy:
    #       Version: "2012-10-17"
    #       Statement:
    #         - Sid: "GitLabFullAccess"
    #           Effect: "Allow"
    #           Principal: "arn:aws:iam::*:user/gitlab"
    #           Action:
    #             - "s3:GetObject"
    #             - "s3:PutObject"
    #             - "s3:DeleteObject"
    #             - "s3:ListBucket"
    #             - "s3:GetBucketLocation"
    #           Resource:
    #             - "arn:aws:s3:::gitlab-registry"
    #             - "arn:aws:s3:::gitlab-registry/*"
    #   - name: gitlab-artifacts
    #     replication: "001"
    #     rack: "workers-1"
    #     dataCenter: "dc-1"
    #     policy:
    #       Version: "2012-10-17"
    #       Statement:
    #         - Sid: "GitLabFullAccess"
    #           Effect: "Allow"
    #           Principal: "arn:aws:iam::*:user/gitlab"
    #           Action:
    #             - "s3:GetObject"
    #             - "s3:PutObject"
    #             - "s3:DeleteObject"
    #             - "s3:ListBucket"
    #             - "s3:GetBucketLocation"
    #           Resource:
    #             - "arn:aws:s3:::gitlab-artifacts"
    #             - "arn:aws:s3:::gitlab-artifacts/*"
    #   - name: gitlab-uploads
    #     replication: "001"
    #     rack: "workers-1"
    #     dataCenter: "dc-1"
    #     policy:
    #       Version: "2012-10-17"
    #       Statement:
    #         - Sid: "GitLabFullAccess"
    #           Effect: "Allow"
    #           Principal: "arn:aws:iam::*:user/gitlab"
    #           Action:
    #             - "s3:GetObject"
    #             - "s3:PutObject"
    #             - "s3:DeleteObject"
    #             - "s3:ListBucket"
    #             - "s3:GetBucketLocation"
    #           Resource:
    #             - "arn:aws:s3:::gitlab-uploads"
    #             - "arn:aws:s3:::gitlab-uploads/*"
    #   - name: gitlab-packages
    #     replication: "001"
    #     rack: "workers-1"
    #     dataCenter: "dc-1"
    #     policy:
    #       Version: "2012-10-17"
    #       Statement:
    #         - Sid: "GitLabFullAccess"
    #           Effect: "Allow"
    #           Principal: "arn:aws:iam::*:user/gitlab"
    #           Action:
    #             - "s3:GetObject"
    #             - "s3:PutObject"
    #             - "s3:DeleteObject"
    #             - "s3:ListBucket"
    #             - "s3:GetBucketLocation"
    #           Resource:
    #             - "arn:aws:s3:::gitlab-packages"
    #             - "arn:aws:s3:::gitlab-packages/*"
    #   - name: gitlab-backups
    #     replication: "001"
    #     rack: "workers-1"
    #     dataCenter: "dc-1"
    #     policy:
    #       Version: "2012-10-17"
    #       Statement:
    #         - Sid: "GitLabFullAccess"
    #           Effect: "Allow"
    #           Principal: "arn:aws:iam::*:user/gitlab"
    #           Action:
    #             - "s3:GetObject"
    #             - "s3:PutObject"
    #             - "s3:DeleteObject"
    #             - "s3:ListBucket"
    #             - "s3:GetBucketLocation"
    #           Resource:
    #             - "arn:aws:s3:::gitlab-backups"
    #             - "arn:aws:s3:::gitlab-backups/*"
    #   # --- GitLab-Runner cache (1) — owned by 'gitlab-runner' user ---
    #   - name: gitlab-runner-cache
    #     replication: "001"
    #     rack: "workers-1"
    #     dataCenter: "dc-1"
    #     policy:
    #       Version: "2012-10-17"
    #       Statement:
    #         - Sid: "GitLabRunnerFullAccess"
    #           Effect: "Allow"
    #           Principal: "arn:aws:iam::*:user/gitlab-runner"
    #           Action:
    #             - "s3:GetObject"
    #             - "s3:PutObject"
    #             - "s3:DeleteObject"
    #             - "s3:ListBucket"
    #             - "s3:GetBucketLocation"
    #           Resource:
    #             - "arn:aws:s3:::gitlab-runner-cache"
    #             - "arn:aws:s3:::gitlab-runner-cache/*"
    # ------
    # --- Pre-built opt-in для Loki (mon-system) bucket ---
    # Если используете SeaweedFS как S3 backend для Loki — раскомментируйте этот блок
    # (объедините с gitlab-блоком выше в ОДИН seaweedfs_sync_buckets_extra, если нужны оба).
    # 1 bucket 'loki-logs' (ДОЛЖЕН совпадать с mon_system_loki_s3_bucket) с full-access
    # policy для 'loki' user. Collection='loki', replication='001'.
    # seaweedfs_sync_buckets_extra:
    #   - name: loki-logs
    #     replication: "001"
    #     rack: "workers-1"
    #     dataCenter: "dc-1"
    #     policy:
    #       Version: "2012-10-17"
    #       Statement:
    #         - Sid: "LokiFullAccess"
    #           Effect: "Allow"
    #           Principal: "arn:aws:iam::*:user/loki"
    #           Action:
    #             - "s3:GetObject"
    #             - "s3:PutObject"
    #             - "s3:DeleteObject"
    #             - "s3:ListBucket"
    #             - "s3:GetBucketLocation"
    #           Resource:
    #             - "arn:aws:s3:::loki-logs"
    #             - "arn:aws:s3:::loki-logs/*"
