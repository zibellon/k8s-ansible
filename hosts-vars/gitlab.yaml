all:
  vars:
    gitlab_namespace: "gitlab"
    # Helm repository - источник chart (переопределить в hosts-vars-override/ для airgap)
    gitlab_helm_is_oci: false
    gitlab_helm_url: "https://charts.gitlab.io"
    gitlab_helm_repo_name: "gitlab"
    gitlab_helm_chart_name: "gitlab"
    gitlab_helm_chart_version: "8.11.8"  # GitLab 17.11
    # Helm timeouts
    gitlab_pre_helm_timeout: "5m"

    gitlab_postgresql_helm_timeout: "5m"
    gitlab_postgresql_rollout_timeout: "300s"

    gitlab_redis_helm_timeout: "5m"
    gitlab_redis_rollout_timeout: "300s"

    gitlab_minio_helm_timeout: "5m"
    gitlab_minio_rollout_timeout: "300s"

    gitlab_install_helm_timeout: "30m"
    gitlab_rollout_timeout: "900s"

    gitlab_post_helm_timeout: "5m"

    # Base domain - используется для генерации внутренних URL (CI/CD переменные, clone URL, webhooks)
    # https: true - потому что внешний трафик приходит по HTTPS (TLS терминируется на Traefik + cert-manager)
    gitlab_ssh_port_external: 3714
    gitlab_ssh_port_internal: 2222 # WARNING: DO NOT CHANGE - hardcoded in charts
    gitlab_ui_domain: "gitlab-k8s-v2.drawapp.ru"
    gitlab_base_domain: "gitlab-k8s-v2.drawapp.ru"
    gitlab_external_https: true
    gitlab_ui_https_secret_name: "cert-gitlab-ui"
    gitlab_registry_domain: "gitlab-registry-k8s-v2.drawapp.ru"
    gitlab_registry_https_secret_name: "cert-gitlab-registry"
    gitlab_pages_domain: "gitlab-pages-k8s-v2.drawapp.ru"
    gitlab_pages_https_secret_name: "cert-gitlab-pages"
    gitlab_minio_console_domain: "gitlab-minio-k8s-v2.drawapp.ru"
    gitlab_minio_console_https_secret_name: "cert-gitlab-minio-console"
    gitlab_minio_api_domain: "gitlab-minio-api-k8s-v2.drawapp.ru"
    gitlab_minio_api_https_secret_name: "cert-gitlab-minio-api"
    # ---
    gitlab_ingress_class_name: "traefik-lb"
    gitlab_vpn_only_enabled: false
    # cert-manager Issuer (per-component, namespaced) — единый объект {enabled, body}.
    # Один на весь gitlab (все 5 доменов: UI/registry/pages/minio-api/minio-console).
    # enabled: true  → Issuer + solver-NetworkPolicies (pre); Certificate доступен (post).
    # enabled: false → ни Issuer, ни solver-NP, ни Certificate.
    gitlab_cert_manager_issuer:
      enabled: true
      body:
        name: "gitlab-acme"
        spec:
          acme:
            server: "https://acme-v02.api.letsencrypt.org/directory"
            email: "some-email-123@gmail.com"
            privateKeySecretRef:
              name: "gitlab-acme-account-key"
            solvers:
              - http01:
                  ingress:
                    ingressClassName: "traefik-lb"
                    ingressTemplate:
                      metadata:
                        annotations:
                          "kubernetes.io/ingress.class": "traefik-lb"
                          "traefik.ingress.kubernetes.io/router.entrypoints": "web"
                    podTemplate:
                      metadata:
                        labels:
                          app.kubernetes.io/name: acme-solver
                          app.kubernetes.io/instance: traefik-lb
                          app.kubernetes.io/component: acme-http01-solver
                          app.kubernetes.io/managed-by: cert-manager
    # Ingress config (per-domain) — независимые toggle'ы ingress / certificate.
    # 1 domain === 1 ingress_config. gitlab — 5 доменов.
    gitlab_ui_ingress_config:
      domain: "{{ gitlab_ui_domain }}"
      tlsSecretName: "{{ gitlab_ui_https_secret_name }}"
      ingress:
        enabled: true
        ingressClass: "{{ gitlab_ingress_class_name }}"
        tlsEnabled: true
        vpnOnlyEnabled: "{{ gitlab_vpn_only_enabled }}"
        vpnIngressMiddlewares: "{{ vpn_ingress_middlewares }}"
      certificate:
        enabled: true
    gitlab_registry_ingress_config:
      domain: "{{ gitlab_registry_domain }}"
      tlsSecretName: "{{ gitlab_registry_https_secret_name }}"
      ingress:
        enabled: true
        ingressClass: "{{ gitlab_ingress_class_name }}"
        tlsEnabled: true
        vpnOnlyEnabled: "{{ gitlab_vpn_only_enabled }}"
        vpnIngressMiddlewares: "{{ vpn_ingress_middlewares }}"
      certificate:
        enabled: true
    gitlab_pages_ingress_config:
      domain: "{{ gitlab_pages_domain }}"
      tlsSecretName: "{{ gitlab_pages_https_secret_name }}"
      ingress:
        enabled: true
        ingressClass: "{{ gitlab_ingress_class_name }}"
        tlsEnabled: true
        vpnOnlyEnabled: "{{ gitlab_vpn_only_enabled }}"
        vpnIngressMiddlewares: "{{ vpn_ingress_middlewares }}"
      certificate:
        enabled: true
    gitlab_minio_api_ingress_config:
      domain: "{{ gitlab_minio_api_domain }}"
      tlsSecretName: "{{ gitlab_minio_api_https_secret_name }}"
      ingress:
        enabled: true
        ingressClass: "{{ gitlab_ingress_class_name }}"
        tlsEnabled: true
        vpnOnlyEnabled: "{{ gitlab_vpn_only_enabled }}"
        vpnIngressMiddlewares: "{{ vpn_ingress_middlewares }}"
      certificate:
        enabled: true
    gitlab_minio_console_ingress_config:
      domain: "{{ gitlab_minio_console_domain }}"
      tlsSecretName: "{{ gitlab_minio_console_https_secret_name }}"
      ingress:
        enabled: true
        ingressClass: "{{ gitlab_ingress_class_name }}"
        tlsEnabled: true
        vpnOnlyEnabled: "{{ gitlab_vpn_only_enabled }}"
        vpnIngressMiddlewares: "{{ vpn_ingress_middlewares }}"
      certificate:
        enabled: true

    # GitLab PostgreSQL
    gitlab_postgresql_image: "docker.io/postgres:16-alpine"
    gitlab_postgresql_storage_class: "lnstr-major-multi-sync"
    gitlab_postgresql_storage_size: "4Gi"
    gitlab_postgresql_username: "gitlab"
    gitlab_postgresql_database_name: "gitlabhq_production"
    gitlab_postgresql_secret_key_username: "username"
    gitlab_postgresql_secret_key_password: "password"

    # GitLab Redis
    gitlab_redis_image: "docker.io/redis/redis-stack-server:latest"
    gitlab_redis_storage_class: "lnstr-major-local"
    gitlab_redis_storage_size: "1Gi"
    gitlab_redis_secret_key_password: "password"
    gitlab_redis_secret_key_redis_args: "redis_args"

    # GitLab MinIO
    gitlab_minio_image: "docker.io/minio/minio:latest"
    gitlab_minio_root_user: "admin"
    gitlab_minio_storage_class: "lnstr-major-local"
    gitlab_minio_storage_size: "2Gi"
    gitlab_minio_root_secret_key_username: "username"
    gitlab_minio_root_secret_key_password: "password"
    # MinIO bucket names
    gitlab_minio_registry_bucket: "registry"
    gitlab_minio_artifacts_bucket: "gitlab-artifacts"
    gitlab_minio_uploads_bucket: "gitlab-uploads"
    gitlab_minio_packages_bucket: "gitlab-packages"
    gitlab_minio_backups_bucket: "gitlab-backups"

    # GitLab MinIO S3 connection settings (used by ESO template for gitlab_minio_connection secret)
    gitlab_minio_s3_endpoint: "http://gitlab-minio:9000"
    gitlab_minio_s3_region: "us-east-1"
    gitlab_minio_s3_path_style: true
    gitlab_minio_registry_secret_key_access_key: "access_key"
    gitlab_minio_registry_secret_key_secret_key: "secret_key"
    gitlab_minio_registry_secret_key_bucket_name: "bucket_name"

    # GitLab Gitaly storage
    gitlab_gitaly_storage_size: "4Gi"
    gitlab_gitaly_storage_class: "lnstr-major-local"

    # ---
    # ---
    # extraObjects (operator-side, default empty) — добавление произвольных K8s resources
    gitlab_pre_extra_objects: []
    # Pre-phase kustomize patches (operator-side customization, default empty)
    gitlab_pre_kustomize_patches: []
    # Pre-phase values (ESO Resources + NetworkPolicies chart)
    # eso_vault_integration_gitlab_secrets — inline merge base + extra (задаётся ниже в eso_vault_integration_gitlab_secrets)
    gitlab_pre_helm_values:
      namespace: "{{ gitlab_namespace }}"
      externalSecrets:
        namespace: "{{ external_secrets_namespace }}"
      traefik:
        namespace: "{{ traefik_namespace }}"
        instanceName: "traefik-{{ traefik_namespace }}"
      haproxy:
        namespace: "{{ haproxy_namespace }}"
      gitlab:
        releaseName: gitlab
        sshPortExternal: "{{ gitlab_ssh_port_external }}"
        sshPortInternal: "{{ gitlab_ssh_port_internal }}"
        vpnOnlyEnabled: "{{ gitlab_vpn_only_enabled }}"
        vpnIps: "{{ vpn_ips }}"
      eso:
        vaultServerUrl: "{{ vault_internal_url }}"
        saName: "{{ eso_vault_integration_gitlab.sa_name }}"
        roleName: "{{ eso_vault_integration_gitlab.role_name }}"
        secretStoreName: "{{ eso_vault_integration_gitlab.secret_store_name }}"
        kvEnginePath: "{{ eso_vault_integration_gitlab.kv_engine_path }}"
        secrets: "{{ eso_vault_integration_gitlab_secrets + (eso_vault_integration_gitlab_secrets_extra | default([])) }}"
      esoResourcesConfig:
        secretStoreRefreshInterval: "{{ eso_resources_config.secret_store_refresh_interval }}"
        externalSecretRefreshInterval: "{{ eso_resources_config.external_secret_refresh_interval }}"
        externalSecretRefreshPolicy: "{{ eso_resources_config.external_secret_refresh_policy }}"
      minio:
        s3Endpoint: "{{ gitlab_minio_s3_endpoint }}"
        s3Region: "{{ gitlab_minio_s3_region }}"
        s3PathStyle: "{{ gitlab_minio_s3_path_style }}"
      issuer: "{{ gitlab_cert_manager_issuer }}"
      extraObjects: "{{ gitlab_pre_extra_objects }}"
    # ---
    # ---
    # extraObjects (operator-side, default empty) — добавление произвольных K8s resources
    gitlab_postgresql_extra_objects: []
    # Postgresql-phase kustomize patches (operator-side customization, default empty)
    gitlab_postgresql_kustomize_patches: []
    # PostgreSQL sub-release values
    # secretName берётся из named-переменной gitlab_secret_postgresql_creds.body.target.name
    gitlab_postgresql_helm_values:
      namespace: "{{ gitlab_namespace }}"
      databaseName: "{{ gitlab_postgresql_database_name }}"
      image: "{{ gitlab_postgresql_image }}"
      storageClass: "{{ gitlab_postgresql_storage_class }}"
      storageSize: "{{ gitlab_postgresql_storage_size }}"
      credentials:
        secretName: "{{ gitlab_secret_postgresql_creds.body.target.name }}"
        usernameKey: "{{ gitlab_postgresql_secret_key_username }}"
        passwordKey: "{{ gitlab_postgresql_secret_key_password }}"
      extraObjects: "{{ gitlab_postgresql_extra_objects }}"
    # ---
    # ---
    # extraObjects (operator-side, default empty) — добавление произвольных K8s resources
    gitlab_redis_extra_objects: []
    # Redis-phase kustomize patches (operator-side customization, default empty)
    gitlab_redis_kustomize_patches: []
    # Redis sub-release values
    # credentialsSecretName берётся из named-переменной gitlab_secret_redis_creds.body.target.name
    gitlab_redis_helm_values:
      namespace: "{{ gitlab_namespace }}"
      image: "{{ gitlab_redis_image }}"
      credentialsSecretName: "{{ gitlab_secret_redis_creds.body.target.name }}"
      credentialsSecretKey: "{{ gitlab_redis_secret_key_redis_args }}"
      storageClass: "{{ gitlab_redis_storage_class }}"
      storageSize: "{{ gitlab_redis_storage_size }}"
      extraObjects: "{{ gitlab_redis_extra_objects }}"
    # ---
    # ---
    # extraObjects (operator-side, default empty) — добавление произвольных K8s resources
    gitlab_minio_extra_objects: []
    # Minio-phase kustomize patches (operator-side customization, default empty)
    gitlab_minio_kustomize_patches: []
    # MinIO sub-release values
    # rootSecretName берётся из named-переменной gitlab_secret_minio_root_creds.body.target.name
    gitlab_minio_helm_values:
      namespace: "{{ gitlab_namespace }}"
      image: "{{ gitlab_minio_image }}"
      storageClass: "{{ gitlab_minio_storage_class }}"
      storageSize: "{{ gitlab_minio_storage_size }}"
      rootSecretName: "{{ gitlab_secret_minio_root_creds.body.target.name }}"
      rootSecretUsernameKey: "{{ gitlab_minio_root_secret_key_username }}"
      rootSecretPasswordKey: "{{ gitlab_minio_root_secret_key_password }}"
      extraObjects: "{{ gitlab_minio_extra_objects }}"
    # ---
    # ---
    # Helm chart values
    gitlab_helm_values:
      global:
        edition: ce
        # Версия образов GitLab — должна соответствовать gitlab_helm_chart_version (8.11.8 = GitLab 17.11)
        gitlabVersion: "17.11.7"
        hosts:
          domain: "{{ gitlab_base_domain }}"
          https: "{{ gitlab_external_https }}"
          gitlab:
            name: "{{ gitlab_ui_domain }}"
            https: "{{ gitlab_external_https }}"
          registry:
            name: "{{ gitlab_registry_domain }}"
            https: "{{ gitlab_external_https }}"
          pages:
            name: "{{ gitlab_pages_domain }}"
            https: "{{ gitlab_external_https }}"
        ingress:
          enabled: false
          configureCertmanager: false
        shell:
          port: "{{ gitlab_ssh_port_external }}"
        psql:
          host: gitlab-postgresql
          port: 5432
          database: "{{ gitlab_postgresql_database_name }}"
          username: "{{ gitlab_postgresql_username }}"
          password:
            useSecret: true
            secret: "{{ gitlab_secret_postgresql_creds.body.target.name }}"
            key: "{{ gitlab_postgresql_secret_key_password }}"
        redis:
          host: gitlab-redis
          port: 6379
          auth:
            enabled: true
            secret: "{{ gitlab_secret_redis_creds.body.target.name }}"
            key: password
        minio:
          enabled: false
        registry:
          bucket: "{{ gitlab_minio_registry_bucket }}"
        kas:
          enabled: false
        pages:
          enabled: true
        appConfig:
          lfs:
            enabled: false
          object_store:
            enabled: true
            proxy_download: true
            connection:
              secret: "{{ gitlab_secret_backup_s3_connection_config.body.target.name }}"
              key: connection
          artifacts:
            enabled: true
            bucket: "{{ gitlab_minio_artifacts_bucket }}"
          uploads:
            enabled: true
            bucket: "{{ gitlab_minio_uploads_bucket }}"
          packages:
            enabled: true
            bucket: "{{ gitlab_minio_packages_bucket }}"
          backups:
            bucket: "{{ gitlab_minio_backups_bucket }}"
        smtp:
          enabled: false
        monitoring:
          enabled: true
      nginx-ingress:
        enabled: false
      certmanager:
        install: false
      prometheus:
        install: false
      postgresql:
        install: false
      redis:
        install: false
      registry:
        enabled: true
        tolerations: []
        nodeSelector: {}
        resources: {}
        metrics:
          enabled: true
          serviceMonitor:
            enabled: true
            endpointConfig:
              interval: "30s"
              scrapeTimeout: "10s"
        hpa:
          minReplicas: 1
          maxReplicas: 1
        maxUnavailable: 1
        deployment:
          strategy:
            type: Recreate
        storage:
          secret: "{{ gitlab_secret_registry_connection_config.body.target.name }}"
          key: config
          redirect:
            disable: true
      gitlab:
        webservice:
          tolerations: []
          nodeSelector: {}
          minReplicas: 1
          maxReplicas: 1
          maxUnavailable: 1
          deployment:
            strategy:
              type: Recreate
          workerProcesses: 2
          workerTimeout: 180
          resources:
            requests:
              memory: "2Gi"
              cpu: "250m"
            limits:
              memory: "4Gi"
          metrics:
            serviceMonitor:
              enabled: true
              endpointConfig:
                interval: "30s"
                scrapeTimeout: "10s"
          workhorse:
            metrics:
              enabled: true
              serviceMonitor:
                enabled: true
                endpointConfig:
                  interval: "30s"
                  scrapeTimeout: "10s"
        sidekiq:
          tolerations: []
          nodeSelector: {}
          minReplicas: 1
          maxReplicas: 1
          maxUnavailable: 1
          deployment:
            strategy:
              type: Recreate
          concurrency: 10
          resources:
            requests:
              memory: "1Gi"
              cpu: "250m"
            limits:
              memory: "3Gi"
          metrics:
            enabled: true
            podMonitor:
              enabled: true
              endpointConfig:
                interval: "30s"
                scrapeTimeout: "10s"
        gitlab-shell:
          tolerations: []
          nodeSelector: {}
          resources: {}
          minReplicas: 1
          maxReplicas: 1
          maxUnavailable: 1
          deployment:
            strategy:
              type: Recreate
        gitaly:
          tolerations: []
          nodeSelector: {}
          resources: {}
          persistence:
            size: "{{ gitlab_gitaly_storage_size }}"
            storageClass: "{{ gitlab_gitaly_storage_class }}"
          metrics:
            enabled: true
            serviceMonitor:
              enabled: true
              endpointConfig:
                interval: "30s"
                scrapeTimeout: "10s"
        gitlab-exporter:
          tolerations: []
          nodeSelector: {}
          resources: {}
          deployment:
            strategy:
              type: Recreate
          metrics:
            serviceMonitor:
              enabled: true
              endpointConfig:
                interval: "30s"
                scrapeTimeout: "10s"
        gitlab-pages:
          tolerations: []
          nodeSelector: {}
          resources: {}
          hpa:
            minReplicas: 1
            maxReplicas: 1
          deployment:
            strategy:
              type: Recreate
          metrics:
            serviceMonitor:
              enabled: true
              endpointConfig:
                interval: "30s"
                scrapeTimeout: "10s"
        toolbox:
          enabled: true
          tolerations: []
          nodeSelector: {}
          resources: {}
          deployment:
            strategy:
              type: Recreate
        migrations:
          enabled: true
          tolerations: []
          nodeSelector: {}
          resources: {}
      gitlab-runner:
        install: false
      upgradeCheck:
        enabled: true
    # ---
    # ---
    # extraObjects (operator-side, default empty) — добавление произвольных K8s resources
    gitlab_post_extra_objects: []
    # Post-phase kustomize patches (operator-side customization, default empty)
    gitlab_post_kustomize_patches: []
    # Post-phase values (Ingress chart)
    gitlab_post_helm_values:
      namespace: "{{ gitlab_namespace }}"
      issuer: "{{ gitlab_cert_manager_issuer }}"
      uiIngressConfig: "{{ gitlab_ui_ingress_config }}"
      registryIngressConfig: "{{ gitlab_registry_ingress_config }}"
      pagesIngressConfig: "{{ gitlab_pages_ingress_config }}"
      minioApiIngressConfig: "{{ gitlab_minio_api_ingress_config }}"
      minioConsoleIngressConfig: "{{ gitlab_minio_console_ingress_config }}"
      sshPortExternal: "{{ gitlab_ssh_port_external }}"
      traefik:
        namespace: "{{ traefik_namespace }}"
        entryPoints:
          web: "{{ traefik_web_entrypoint }}"
          websecure: "{{ traefik_websecure_entrypoint }}"
      haproxy:
        namespace: "{{ haproxy_namespace }}"
        ingressClass: "{{ haproxy_ingress_class }}"
      extraObjects: "{{ gitlab_post_extra_objects }}"

    # ---------
    # ESO integration: Vault → K8s Secrets
    # Базовые секреты (нужны для запуска) — отдельные named-переменные с полным dict-объектом.
    # eso_vault_integration_gitlab_secrets — массив ссылок на named-переменные
    # (chart eso-external-secret.yaml рендерит .Values.eso.secrets единообразно
    # как для base-ссылок, так и для _extra dict'ов).
    # _extra — расширение оператора через hosts-vars-override/.
    # ---------
    eso_vault_integration_gitlab:
      sa_name: "eso-main"
      role_name: "gitlab.eso-main"
      secret_store_name: "eso-main.vault"
      kv_engine_path: "eso-secret"

    gitlab_secret_postgresql_creds:
      external_secret_name: "eso-gitlab-postgresql-creds"
      vault_path: "/gitlab/postgresql/creds"
      body:
        target:
          name: "eso-gitlab-postgresql-creds"
        dataFrom:
          - extract:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/postgresql/creds"

    gitlab_secret_redis_creds:
      external_secret_name: "eso-gitlab-redis-creds"
      vault_path: "/gitlab/redis/creds"
      body:
        target:
          name: "eso-gitlab-redis-creds"
        dataFrom:
          - extract:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/redis/creds"

    gitlab_secret_minio_root_creds:
      external_secret_name: "eso-gitlab-minio-root-creds"
      vault_path: "/gitlab/minio/root"
      body:
        target:
          name: "eso-gitlab-minio-root-creds"
        dataFrom:
          - extract:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/minio/root"

    gitlab_secret_minio_registry_creds:
      external_secret_name: "eso-gitlab-minio-registry-creds"
      vault_path: "/gitlab/minio/registry"
      body:
        target:
          name: "eso-gitlab-minio-registry-creds"
        dataFrom:
          - extract:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/minio/registry"

    # SPECIAL: registry_connection_config uses ESO template to generate registry storage config
    gitlab_secret_registry_connection_config:
      external_secret_name: "eso-gitlab-registry-connection-config"
      vault_path: "/gitlab/minio/registry"
      body:
        target:
          name: "eso-gitlab-registry-connection-config"
          template:
            engineVersion: v2
            data:
              config: |
                s3:
                  bucket: {{ gitlab_minio_registry_bucket }}
                  accesskey: {% raw %}{{ .access_key }}{% endraw +%}
                  secretkey: {% raw %}{{ .secret_key }}{% endraw +%}
                  regionendpoint: {{ gitlab_minio_s3_endpoint }}
                  region: {{ gitlab_minio_s3_region }}
                  v4auth: true
        data:
          - secretKey: access_key
            remoteRef:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/minio/registry"
              property: "{{ gitlab_minio_registry_secret_key_access_key }}"
          - secretKey: secret_key
            remoteRef:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/minio/registry"
              property: "{{ gitlab_minio_registry_secret_key_secret_key }}"

    # SPECIAL: backup_s3_connection_config uses ESO template to generate s3_connection YAML
    gitlab_secret_backup_s3_connection_config:
      external_secret_name: "eso-gitlab-backup-s3-connection-config"
      vault_path: "/gitlab/minio/registry"
      body:
        target:
          name: "eso-gitlab-backup-s3-connection-config"
          template:
            engineVersion: v2
            data:
              connection: |
                provider: AWS
                region: {{ gitlab_minio_s3_region }}
                endpoint: {{ gitlab_minio_s3_endpoint }}
                path_style: {{ gitlab_minio_s3_path_style | lower }}
                aws_access_key_id: {% raw %}{{ .access_key }}{% endraw +%}
                aws_secret_access_key: {% raw %}{{ .secret_key }}{% endraw +%}
        data:
          - secretKey: access_key
            remoteRef:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/minio/registry"
              property: "{{ gitlab_minio_registry_secret_key_access_key }}"
          - secretKey: secret_key
            remoteRef:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/minio/registry"
              property: "{{ gitlab_minio_registry_secret_key_secret_key }}"

    gitlab_root_secret_key_username: "username"
    gitlab_root_secret_key_password: "password"
    gitlab_root_secret_key_token: "token"
    # PINNED — GitLab root user фиксирован, менять нельзя
    gitlab_root_username: "root"
    # never rendered (is_need_eso: false) — body присутствует для консистентности схемы
    gitlab_secret_root_creds:
      external_secret_name: "eso-gitlab-root-creds"
      vault_path: "/gitlab/root/creds"
      is_need_eso: false
      body:
        target:
          name: "eso-gitlab-root-creds"
        dataFrom:
          - extract:
              key: "{{ eso_vault_integration_gitlab.kv_engine_path }}/data/gitlab/root/creds"

    eso_vault_integration_gitlab_secrets:
      - "{{ gitlab_secret_postgresql_creds }}"
      - "{{ gitlab_secret_redis_creds }}"
      - "{{ gitlab_secret_minio_root_creds }}"
      - "{{ gitlab_secret_minio_registry_creds }}"
      - "{{ gitlab_secret_registry_connection_config }}"
      - "{{ gitlab_secret_backup_s3_connection_config }}"
      - "{{ gitlab_secret_root_creds }}"

    eso_vault_integration_gitlab_secrets_extra: []
