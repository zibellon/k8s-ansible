# Namespace
namespace: ""

# DNS domain for cluster services
dnsDomain: "cluster.local"

# PostgreSQL connection settings
postgres:
  username: "postgres"
  password: ""
  port: 5432
  database: "infisical"

# Redis connection settings
redis:
  password: ""
  port: 6379

# Infisical secrets (set via playbook)
infisicalSecrets:
  authSecret: ""
  encryptionKey: ""
  siteUrl: ""
