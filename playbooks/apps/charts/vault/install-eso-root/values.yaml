# DNS domain for internal service resolution
dnsDomain: ""

# Vault settings
vault:
  namespace: ""
  kvMountPath: "secret"  # DO NOT CHANGE - standard path for KV secrets engine
  selfCredsPath: "vault"  # Path where Vault stores its own credentials

# ESO root authentication settings
esoRoot:
  policyName: ""
  roleName: ""
  saName: ""

# SecretStore settings
secretStore:
  name: "vault-root"

# ExternalSecret settings
externalSecret:
  name: ""

# Target Kubernetes Secret
targetSecret:
  name: ""
