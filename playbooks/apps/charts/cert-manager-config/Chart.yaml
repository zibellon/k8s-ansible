# =============================================================================
# Helm chart for cert-manager configuration
# Includes: NetworkPolicy + ClusterIssuer
# =============================================================================

apiVersion: v2
name: cert-manager-config
description: NetworkPolicy and ClusterIssuer for cert-manager
type: application
version: 0.1.0
appVersion: "1.0.0"

