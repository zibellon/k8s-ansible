# HAProxy Install

This directory is a placeholder. HAProxy is installed from the official Helm chart:

CRDs are installed automatically with the official chart.

See [haproxy-install.yaml](../../../haproxy-install.yaml) for the full installation playbook.
