# =============================================================================
# SERVICE ACCOUNTS FOR VAULT AUTHENTICATION
# =============================================================================

---
# ServiceAccount for GitLab secrets
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.gitlabEso.saName }}
  namespace: {{ .Values.namespace }}

---
# ServiceAccount for GitLab MinIO secrets
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.gitlabMinioEso.saName }}
  namespace: {{ .Values.namespace }}
